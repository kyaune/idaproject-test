export const ProductCard = () => import('../../../../components/product_card.vue' /* webpackChunkName: "components/product-card" */).then(c => wrapFunctional(c.default || c))
export const ProductsList = () => import('../../../../components/products_list.vue' /* webpackChunkName: "components/products-list" */).then(c => wrapFunctional(c.default || c))
export const Sidemenu = () => import('../../../../components/sidemenu.vue' /* webpackChunkName: "components/sidemenu" */).then(c => wrapFunctional(c.default || c))
export const UiButton = () => import('../../../../components/ui/button.vue' /* webpackChunkName: "components/ui-button" */).then(c => wrapFunctional(c.default || c))
export const UiDeleteIcon = () => import('../../../../components/ui/delete_icon.vue' /* webpackChunkName: "components/ui-delete-icon" */).then(c => wrapFunctional(c.default || c))

// nuxt/nuxt.js#8607
function wrapFunctional(options) {
  if (!options || !options.functional) {
    return options
  }

  const propKeys = Array.isArray(options.props) ? options.props : Object.keys(options.props || {})

  return {
    render(h) {
      const attrs = {}
      const props = {}

      for (const key in this.$attrs) {
        if (propKeys.includes(key)) {
          props[key] = this.$attrs[key]
        } else {
          attrs[key] = this.$attrs[key]
        }
      }

      return h(options, {
        on: this.$listeners,
        attrs,
        props,
        scopedSlots: this.$scopedSlots,
      }, this.$slots.default)
    }
  }
}
